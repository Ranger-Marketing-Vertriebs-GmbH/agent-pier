# File explorer UX

The user approved the contextual screenshots on 2026-09-17 and requested implementation. Keep the real AgentPier shell, navigation and session tabs. Redesign only the global Files page and session Files tab using the shared explorer. The global explorer browses the server filesystem; sessions remain scoped to their working directory.

Use existing dark surfaces and orange accents. Show compact breadcrumbs, New and Upload actions, search, and a spacious file list. Hide direct path editing, advanced search and view controls behind explicit disclosures. Keep directory shortcuts on desktop and a Places dialog on mobile. Do not invent a remote hostname: expose the existing scope/root truthfully.

Show file actions contextually, keep keyboard shortcuts, multi-selection, read-only enforcement, clipboard, archives, upload recovery and job conflicts. Collapse idle uploads/job history behind controls; do not unmount their state or hide conflicts/errors without an indicator. Search results remain accessible. Preview is the primary file view and metadata lives in Details. On mobile show the opened file instead of the list, with a back action restoring navigation. Keep the editor and dirty-document guards intact.

Maintain matching German/English catalogs. No new dependencies or backend contract changes. Source files must stay <=600 lines. Validate with regression browser tests in Chromium/WebKit, catalog tests, lint, formatting, structure, build and backend tests. Capture real screenshots for the PR. Work in a feature worktree; submit a PR, do not merge without CI and review resolution.

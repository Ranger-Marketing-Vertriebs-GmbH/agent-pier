/** English counterpart of de/cli-profiles.js with identical keys. */
export const cliProfiles = Object.freeze({
  importFileLimit:
    "Extension import exceeds its file limit; existing files were retained.",
  unexpectedDirectory: "An extension directory points to an unexpected location.",
  shellNoExtensions: "Shell has no shared CLI extensions.",
});
